BACKEND_URL=http://localhost:8080

# PostHog analytics (optional — telemetry is disabled when unset)
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=
POSTHOG_KEY=
POSTHOG_HOST=

# Public origin for absolute URLs in sitemaps (optional — sitemap endpoints return 404 when unset)
PUBLIC_ORIGIN=
