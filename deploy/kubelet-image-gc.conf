# Evicts unused images on age rather than waiting for disk pressure, which a 47%-full root
# filesystem never reaches. Install as a kubelet drop-in; imageMaximumGCAge has no CLI flag.
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
imageMaximumGCAge: 168h
