# The one place the Rust version is written down. CI reads `channel` from here and
# installs exactly it, so a lint that fires in the pipeline fires locally too; a
# floating `stable` silently gains lints between two runs of the same commit.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
